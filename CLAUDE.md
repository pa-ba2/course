# Course project — working conventions

## Verification policy
Do not start a local server, open the Browser pane, or screenshot/verify a page after routine edits by default.
Only verify when the user explicitly asks for it, or when there's a specific reason to suspect the edit broke something (e.g. an error surfaced, a structural change to shared template/CSS, or the user reports something looks wrong).
